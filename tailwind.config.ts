/**
 * Legacy Tailwind config file kept ONLY for shadcn/ui tooling references.
 *
 * Tailwind CSS v4.1 is configured CSS-first in `app/globals.css` using:
 * - `@theme` for tokens (colors/radius/animations)
 * - `@plugin` for plugins
 * - `@utility` for container behavior
 *
 * Do not add theme or plugin configuration here — it will not be picked up unless
 * you explicitly load it via `@config` (which we intentionally do NOT use).
 */
export default {}
