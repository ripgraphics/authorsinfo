// Update the prop types with correct naming
export interface ProfilePageProps {
  params: {
    id: string
  }
}

export interface PublisherPageProps {
  params: {
    id: string
  }
}

export interface AuthorPageProps {
  params: {
    id: string
  }
}

export interface BookPageProps {
  params: {
    id: string
  }
}
